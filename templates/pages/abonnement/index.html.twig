{% extends '../../base.html.twig' %}

{% block title %}Hello AbonnementController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    nav{
            margin: 0;
            left: 0;
            top: 0;
            position: absolute;
            width: 100%;
            height: 10%;
            font-size: 25px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 200px;
            color: #d8f3dc;
        }

        a{
            color: #d8f3dc;
            text-decoration: none;
        }

        a:hover{
            text-decoration: underline;
        }
</style>
<nav>
        {% if app.user %}
            <span>Connecté en tant que {{ app.user.email }}</span>
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
            <a href="/connexion">Connexion</a>
        {% endif %}
        <a href="/abonnement">Abonnements</a>
        <a href="/achat">Achats</a>
        <a href="{{ path('app_register') }}">Inscription</a>

    </nav>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/wamp64/www/Spendings/src/Controller/AbonnementController.php</code></li>
        <li>Your template at <code>C:/wamp64/www/Spendings/templates/abonnement/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
